@model IEnumerable<Server_v0._0.Models.Status>

@{
    ViewData["Title"] = "Index";
}

<h1>Statuses</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr style=@Delete.DelColor(@item.IsDeleted)>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StatusId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StatusId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StatusId">@Delete.DelStatus(@item.IsDeleted)</a>
            </td>
        </tr>
}
    </tbody>
</table>
