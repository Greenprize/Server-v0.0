@model Server_v0._0.Models.Status

@{
    ViewData["Title"] = @Delete.DelStatus(@Model.IsDeleted);
}

<h1>@Delete.DelStatus(@Model.IsDeleted)</h1>

<h3>Are you sure you want to @Delete.DelStatus(@Model.IsDeleted) this?</h3>
<div>
    <h4>Status</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Title)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="StatusId" />
        <input type="submit" value=@Delete.DelStatus(@Model.IsDeleted) class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
